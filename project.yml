name: StudentLifeOS
options:
  bundleIdPrefix: com.studentlife
targets:
  StudentLifeOS:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [Sources]
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        CURRENT_PROJECT_VERSION: "1"
        MARKETING_VERSION: "1.0"
        PRODUCT_BUNDLE_IDENTIFIER: com.studentlife.StudentLifeOS
